/*i8253相当于一个特定频率的时钟源，
  而且输出的时钟信号就当作中断信号挂载在 i8259A 上*/
#include "io.h"

//初始化i8253
void init8253(void) {
	//设置控制字
	outb(0x43, 0x34);
	//设置分频参数59659=0xe90b --> 时钟频率为20Hz
	outb(0x40, 0x0b);
	outb(0x40, 0xe9);
}
